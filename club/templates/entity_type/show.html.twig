{% extends 'base.html.twig' %}

{% block title %}EntityType{% endblock %}

{% block body %}
    <h1>EntityType</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entity_type.id }}</td>
            </tr>
            <tr>
                <th>Price</th>
                <td>{{ entity_type.price }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ entity_type.description }}</td>
            </tr>
            <tr>
                <th>Files</th>
                <td>{{ entity_type.files }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ entity_type.type }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_entity_type_index') }}">back to list</a>

    <a href="{{ path('app_entity_type_edit', {'id': entity_type.id}) }}">edit</a>

    {{ include('entity_type/_delete_form.html.twig') }}
{% endblock %}
